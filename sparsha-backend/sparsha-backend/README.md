
\## Setup

1\. Clone repo

2\. `pip install -r requirements.txt`

3\. Add API keys to `.env` file (inside backend directory)

4\. go to backend directory, run `python app.py`

5\. open another terminal, go to frontend, run `npm install`

6\. then run `npm run dev`
